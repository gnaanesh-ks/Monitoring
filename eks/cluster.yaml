apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: flask-eks-cluster
  region: us-east-1
managedNodeGroups:
- name: ng-general
  instanceType: t3.small
  desiredCapacity: 2
  minSize: 1
  maxSize: 3
  volumeSize: 30

